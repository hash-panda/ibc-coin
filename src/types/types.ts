export interface CoinPair {
    name: string;
    coinPair: string;
    icon: string;
    price: number | string;
    priceUnit: string;
    marketCap: number | string;
    marketCapUnit: string;
    h24: string;
}
